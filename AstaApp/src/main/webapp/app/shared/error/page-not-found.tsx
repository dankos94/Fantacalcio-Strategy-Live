import React from 'react';

import { Alert } from 'reactstrap';

const PageNotFound = () => {
  return (
    <div>
      <Alert color="danger">La pagina non esiste.</Alert>
    </div>
  );
};

export default PageNotFound;
